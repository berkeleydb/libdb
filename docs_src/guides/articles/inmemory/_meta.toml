# Nav/index metadata for the inmemory guide (auto-derived from the
# source index.html TOC chain). `order` pins the reading order for nav
# and later PDF assembly; `landing` is the tree's index page.

title = "Writing In-Memory Berkeley DB Applications"
landing = "index.md"
order = [
]
