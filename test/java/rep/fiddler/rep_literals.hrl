%% repmgr envelope types
%% 
-define(ACK, 1).
-define(HANDSHAKE, 2).
-define(REP_MESSAGE, 3).

%% Replication (control) message types
%% 
-define(NEWCLIENT, 15).
-define(NEWSITE, 18).
-define(PAGE, 19).

-define(REPVERSION_47, 5).
